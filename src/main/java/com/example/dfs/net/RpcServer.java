package com.example.dfs.net;

// In this simplified prototype Spring REST controllers act as the RPC server.
// Keep this placeholder for later extension (gRPC, socket server, etc).
public class RpcServer {
}
